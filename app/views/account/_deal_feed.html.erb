<!--<div>
  <table width="100%" cellpadding="0">
    <tr><td rowspan="2" valign="center" width="40px">
<%= image_tag "icons/web-beer.gif" %>
      </td><td align="left">
<% deal = feed.deal; quantity = deal.spongers.count; -%>
<%= "#{deal.description} za #{deal.cost} zł" %>
      </td><td align="right">
<%= "#{deal.created_at}" %>
<%  if deal.is_enabled %>
  <%= link_to image_tag('icons/cross.png'), :controller => 'deals', :action => 'remove', :id => deal.id%>
<% end %>
    </td></tr>
    <tr><td colspan="2">
        <table cellpadding="0" cellspacing="0" width="100%">
          <tr>
            <td width="1px"><%= "#{deal.sponsor.name}&nbsp;->  " %></td>
            <td>&nbsp;<%= "#{deal.spongers.map{ |s| s.name}.join', '}" %></td>
            <td width="1px">&lt;&lt;<%= "#{deal.snitch.name}" %></td>
          </tr>
        </table>
    </td></tr>
  </table>
</div>-->

<% deal = feed.deal; quantity = deal.spongers.count; -%>
  
  <div class="title" style="font-size:25px;"><%= deal.description %></div>
  <div class="gravatar"><img src="/images/icons/<%= deal.icon.to_s %>.gif"/></div>
  <div class="gravatar" style="font-family:verdana;font-size:12px;"><%= gravatar_for(deal.sponsor, :size => 50) + "<br/>" + deal.sponsor.name %> </div>
  <span class="strzalka"><%= deal.cost %> zł</span>
  <% deal.spongers.each do |sponger| -%>
    <%= gravatar_for(sponger, :size => 50) %>
  <% end -%>
      